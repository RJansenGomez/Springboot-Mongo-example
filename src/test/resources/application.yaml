spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: test-mongoDB

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG
            repository:
              Query: DEBUG